events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1000;
    gzip_types text/plain text/css application/json application/javascript application/xml;

    upstream dashboard {
        server dashboard:8000;
    }

    server {
        listen 80;
        server_name literature-review.example.com;  # Replace with your domain
        
        # Redirect HTTP to HTTPS (comment out if SSL not configured)
        # return 301 https://$server_name$request_uri;
        
        # Or use this for HTTP-only deployment
        location / {
            proxy_pass http://dashboard;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            # Timeouts
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 300s;
        }

        # Max upload size
        client_max_body_size 100M;
    }

    # HTTPS server (uncomment and configure after obtaining SSL certificates)
    # server {
    #     listen 443 ssl http2;
    #     server_name literature-review.yourdomain.com;
    #
    #     ssl_certificate /etc/letsencrypt/live/literature-review.yourdomain.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/literature-review.yourdomain.com/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #
    #     # Security headers
    #     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    #     add_header X-Content-Type-Options "nosniff" always;
    #     add_header X-Frame-Options "SAMEORIGIN" always;
    #
    #     client_max_body_size 100M;
    #
    #     location / {
    #         proxy_pass http://dashboard;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #
    #         # WebSocket support
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #
    #         # Timeouts
    #         proxy_connect_timeout 60s;
    #         proxy_send_timeout 60s;
    #         proxy_read_timeout 300s;
    #     }
    #
    #     # Static files (if any)
    #     location /static/ {
    #         alias /app/webdashboard/static/;
    #         expires 1y;
    #         access_log off;
    #     }
    # }
}
